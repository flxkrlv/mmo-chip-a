# 2026-06-15 — Bug fixes: RE Cell crash & infinite loop

## Утро (09:00-09:40)

### Bugs fixed in CellRERightPanel

1. **`activeCellTypeId is not defined`** — переменная использовалась в `AnalogDeviceRow` при рендере Analog Devices секции, но не была определена в области видимости `CellRERightPanel`. Фикс: добавил чтение `activeCellTypeId` из стора `useCellREStore` (как уже делала левая панель).

2. **Infinite loop в `AnalogDeviceRow`** — zustand селектор `(s) => (s as any).analogOverrides?.[cellTypeId]?.[device.id] ?? {}` создавал новый объект `{}` на каждый рендер, что через `useSyncExternalStore` вызывало бесконечный цикл ре-рендеров. Фикс: вынес стабильный `NO_OVERRIDES: Record<string, number> = {}` в модуль.

### Committed & pushed
- Branch: `analog-re-wip`
- Commit: `Fix RE Cell crash (activeCellTypeId undefined) + infinite loop in AnalogDeviceRow`
- Pushed to fork (origin не доступен — 403).
