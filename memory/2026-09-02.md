# 2026-09-02

## План динамического схемного рендера → docs/
- Написал `docs/interactive-schematic-engine-plan.md` — план интерактивного
  аналогового schematic-канваса (вариант B: свой SVG-канвас как LogicSchematicCanvas,
  ELK placement-only, свои L/Z ortho-роутер + хаб, символы импортируются из
  netlist2svgSkin дословно — 4-term MOS с bulk кастомный, в TSX не переводим).
- Ключевые решения: lock-позиций (локальное состояние + localStorage, слоты по scope)
  — дифференциатор отсутствует у CircuitLLM; нет лимита устройств; тумблер
  static/interactive в SchematicViewPanel; reuse позже в AssistantPanel.
- Исследованы: CircuitFormer (HUST) — цифровой P&R congestion, не для нас;
  EEschematic (eelab-dev, MIT) — детерминированный netlist→wiring builder + per-violation
  repair паттерн; CircuitLLM (fra00) — React Flow + ELK placement-only, НО нет lock/
  персиста позиций (relayout затирает все).
- Реализация НЕ начата — только план в docs/ (запушен bd3002b?).