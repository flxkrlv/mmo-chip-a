# 2026-08-28

## AI Assistant Phase 2 — M1 + M2 выполнены

### M1 (завершено)
- `subcircuitAnalysis.ts` → `assistantSnapshot.ts`; `analyseSubcircuits` → `prepareAssistantSnapshot`. Функция НЕ анализирует субцепи — только нормализует/скопирует снапшот.
- `brief.prompt` (и прочие поля brief) теперь попадают в system-промпт `promptForGraphAnalysis` (`backend/src/assistant/llmGraphAnalysis.ts`), а не только в payload.
- Убран ложный `deterministic` (переменная в `assistant.ts` переименована в `prepared`).
- Тест-файл переименован в `assistantSnapshot.test.ts`; путь в `package.json` test-скрипте обновлён.

### M2 (завершено)
- Backend: `discussFindingWithLlm` (`llmGraphAnalysis.ts`) + роут `POST /api/dies/:dieId/assistant/discuss` (`assistant.ts`). Фокусируется на subgraph находки (deviceUuids + netIds), 429-retry, read-only.
- Shared типы: `AssistantChatMessage`, `AssistantDiscussFinding`, `AssistantDiscussRequest/Response/ErrorResponse`.
- Frontend: `assistantSession.ts` — `findingThreads` + `appendFindingMessage`/`resetFindingThread`; `assistantAnalysis.ts` — `discussFinding`; `AssistantPanel.tsx` — кнопка "Discuss" у каждой карточки + инлайн-чат `FindingChat` (scoped к находке).
- Проверка: backend `tsc` EXIT 0, assistant тесты 3/3, frontend `tsc --noEmit` EXIT 0.

### Отложено / решения
- `mmochip_lvs` — ОТЛОЖЕН (LLM плохо генерит SPICE, нет базы топологий).
- Из тулов M3 остаётся ТОЛЬКО `mmochip_vision` (via/expand выкинуты: via агенту знать не надо, expand избыточен).
- M3: tool-loop с единственным `mmochip_vision`, per-tool селектор (1 чекбокс Vision), кап 4 итерации.

### Статус ветки
- Ветка `analog-re-wip`. v1.7-alpha уже запушен (до M1/M2). M1+M2 пока НЕ закоммичены (пользователь не просил коммит).
