# 2026-06-30 — MOS symbols in netlist2svg skin (debug session)

## Fixed: port label coordinates in generic NMOS/PMOS boxes
- G/S/B labels used absolute SVG coords but were positioned as if relative to port `<g>` elements (which have `s:x/s:y` metadata, not SVG transform)
- Fixed: D=(15,-4), G=(-14,19), S=(15,72), B=(33,49) in 30×60 cell

## Replaced generic boxes with proper MOS symbols (3 iterations)

### Iteration 1: Traditional gate-electrode + body-bars (rejected)
- No, user wanted BJT-like arrow on source.

### Iteration 2: IC-style with arrow on source (rejected — "квадраты какие-то")
- Body plates left of channel, gate plates right with 2px insulation gap
- Arrow on source pointing DOWN for both NMOS/PMOS

### Iteration 3 (final): Based on user's reference SVGs (nmos.svg / pmos.svg)
- **C-shaped gate on RIGHT** (x=20..30, 4px gap from channel at x=16)
- **Body (B) on LEFT**, connects to channel vertical at y=18
- **Horizontal source arrow**: NMOS → right (outward toward G), PMOS ← left (inward toward channel)
- **PMOS bubble** on gate line (cx=25, cy=18, r=2.5)
- **Size**: 32×36 (close to BJT 32×32)
- **NMOS**: D on top, S on bottom, arrow at bottom → right
- **PMOS**: S on top (arrow pointing ← left), D on bottom
- Port directions in format file updated for PMOS swapped D/S

### Files modified
- `frontend/src/lib/schematic/netlist2svgSkin.ts` — NMOS/PMOS definitions
- `frontend/src/lib/schematic/netlist2svgFormat.ts` — portDirections for PMOS

## State
- `tsc --noEmit` passes (0 errors)
- `vite build` succeeds
- User has reference SVGs at `E:\mmo_workdir\mmo-chip-a\frontend\src\lib\schematic\nmos.svg` and `pmos.svg`
- Next: user to rebuild and test visually
## User provided new NMOS/PMOS SVGs — skin update pending

### Files created by user
- `frontend/src/lib/schematic/netlist2svg_nmos.svg` — new NMOS symbol (32×50)
- `frontend/src/lib/schematic/netlist2svg_pmos.svg` — new PMOS symbol (32×50)

### Key visual changes from user's SVGs (vs current skin)
1. **Channel line** added at x=16, vertical from y=6 to y=30, 2px stroke
2. **Body connect** now stops at channel (x≈16) instead of C-shape (x=20)
3. C-shape, arrows, port positions unchanged

### Uncommitted skin update (blocked by memory flush)
The `netlist2svgSkin.ts` file was read and a corrected version was prepared but **could not be written** due to the pre-compaction memory flush restricting writes to memory-only files. The update needs to be applied once the flush period ends.

### Changes to apply later
In `transistor_nmos` and `transistor_pmos` sections of `netlist2svgSkin.ts`:
- ADD: `<path d="M16,6 V30" class="symbol $cell_id"/>` (channel)
- CHANGE body connect: `M0,18 H20` → `M0,18 H16`
- Reorder paths: channel first, then body, then C-shape, then gate connect, then D/S/arrow
- Everything else (ports, arrows, C-shape) stays the same

### Build state
- User needs to rebuild dev server after skin file is updated
- `tsc --noEmit` checked before flush — the change is path-only, should not break compilation
