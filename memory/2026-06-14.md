# 2026-06-14 — Analog RE: Day 2 — Big push (continued)

Поздняя сессия (23:00-00:00+):
- Cell type device review: `AnalogDeviceRow` теперь расширяемый — кнопка "+ override" раскрывает поля для ввода параметров, overrides хранятся в `preferences.analogOverrides` (persist после F5). Подсвечиваются акцентным цветом.
- Layout-oriented export: `layoutExport.ts` — формат `instance,x_um,y_um,rotation,kind,params`. Кнопка "layout" в Netlist tab toolbar. SKILL-шаблон в `docs/mmo_chip_place.il` для Cadence Virtuoso.
- SheetR persistence: был баг — `partialize` в preferences отфильтровывал sheetR. Добавлен в белый список.
- RE Cell wrong cell bug: URL params fallback в auto-select эффекте.
- AnalogDiePanel рефакторинг: удалены устаревшие секции (die-level layers, scan button, CDL preview). SheetR теперь реактивный.
- Resistor type отображается в DeviceInspector (Type: POLY/HSR/PB/NPL/FILM).
- Сопротивление вычисляется динамически из sheetR × squares (DeviceInspector + overlay + RE Cell).

## Проблемы
- Wire matching → intersection отложен (требует bbox из contact shapes)
- SPICE quality pass ждёт эталонного netlist
- Push на fork работает, на origin — нет (403)
