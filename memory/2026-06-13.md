# 2026-06-13 — Analog RE: Full Day Sprint

## Core Work
- **Multi-layer image system** (MVP): OverlayImageLayer with file picker + server auto-load
- **Ruler/Measure tool**: 5 modes (free/h/v/ortho/diag), set scale via double-click, `umPerPx` in annotations
- **Hotkey system**: central `lib/hotkeys.ts`, Die Viewer (s/w/o/k/b/r/p/f/+/-), Cell RE (r/p/o)
- **Per-net colors**: override in preferences, clickable swatch in OutlineTree
- **IO pin snapping**: `findNearestTerminal` includes pins, netlist alignment
- **LPnp + vpnp**: layers added, LPnp PE = emitter perimeter
- **Cell RE**: switched from geometry-based `detectAnalogDevices` to marker-based `extractMarkedDevices`

## Key Bugfixes
- Resistor short: round-robin contact distribution for 2-terminal devices
- BJT wire matching: shared contact filter → priority-based assignment (older layer wins)
- NPN detected as PNP fixed
- Overlay layers duplicated on F5 — fixed with dedup guard
- umPerPx now flows from ruler → annotations → CDL export

## Architecture Decisions
- Multi-layer images stay as static overlay (not tiled) for now — analog die scans <300MB
- Cell RE uses marker-based detection (user draws npn_id/pnp_id/collector/base/emitter)
- Hotkeys are in a single config file, no GUI editor yet
- Net list alignment: wire net names + IO pin names → SPICE port names (documented risk: duplicates = short)

## Git
- Pushed two commits to fork remote
- Large PNGs excluded via .gitignore (test_dies_gitignore/)
